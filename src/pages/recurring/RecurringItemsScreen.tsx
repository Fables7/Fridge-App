import React from 'react';

import {Text} from 'react-native';
import {StyledMain} from '../../sharedStyles';

const RecurringItemsScreen = () => {
  return (
    <StyledMain>
      <Text>Recurring Items Screen WIP</Text>
    </StyledMain>
  );
};

export default RecurringItemsScreen;
